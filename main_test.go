package main

import (
	"testing"
)

// TestMain - пустой тест, необходимый для корректного запуска всех тестов
// из корневой директории проекта через команду go test ./...
func TestMain(t *testing.T) {
	// Этот тест ничего не делает, он нужен только для запуска всех тестов
}
